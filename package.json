{
  "name": "convert_brm_templates",
  "version": "0.1.0",
  "description": "converting brm to templates",
  "main": "index.js",
  "license": "Proprietary",
  "private": true,
  "scripts": {
    "start": "rimraf dist && eleventy --serve",
    "watch": "rimraf dist && eleventy --watch",
    "build": "rimraf dist && eleventy",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist/brm -r https://github.com/isp-tutor-project/brm/"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.1",
    "gh-pages": "^3.2.3",
    "rimraf": "^3.0.2"
  }
}
